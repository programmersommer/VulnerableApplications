@using WorkshopApp.Entities
@using WorkshopApp.ViewModels

@{
    ViewData["Title"] = "Article viewer";
}

<div class="container">
    <div class="row">
        <div class="col-10 offset-1">
            @*Html.Raw is insecure, but we are using trusted source, right?*@
            <p>@Html.Raw(@Model.Text)</p>
        </div>
    </div>
</div>

